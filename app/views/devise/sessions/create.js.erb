$(document).ready(function() {
  $('.modal').modal('hide');
  <% if resource&.setting&.sign_in_redirect_location == 'Profile' %>
    window.location.replace("<%= user_path(username: resource.username) %>");
  <% else %>
    window.location.replace("/");
  <% end %>
})
