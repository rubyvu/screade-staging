$('#search-form-placeholder').html("<%= j render partial: 'search_form' %>")
$('#modal-group-search').modal('show')
$('#modal-group-search').on('hidden.bs.modal', function () {
   window.location.reload();
});
